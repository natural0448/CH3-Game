# 다음 교시 시작 순서

2026-09-15 보완/검증 완료. 검증용 Kafka 3개 프로세스를 종료했고, 8000 및 9092~9097 포트가 모두 닫힌 것을 확인했습니다. 내부적으로 유지하는 Django/publisher/접속기 프로세스도 없습니다. 기존 MySQL Windows 서비스는 그대로 사용합니다.

PowerShell 창 여러 개를 열거나 VS Code의 **터미널 → 새 터미널**을 누르면 됩니다. 실행 중인 서버 터미널에 다음 명령을 이어 입력하지 말고, 역할마다 새 터미널을 사용하세요.

## 스크립트 실행이 차단되는 경우

`이 시스템에서 스크립트를 실행할 수 없으므로 ... kafka.ps1 파일을 로드할 수 없습니다`가 나오면, 해당 터미널에서 아래 한 줄을 실행한 뒤 원래 명령을 다시 실행합니다.

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned -Force
```

현재 터미널에만 적용되고 창을 닫으면 사라집니다. PC 전체나 사용자 계정의 영구 설정은 변경하지 않습니다. Kafka 스크립트를 사용하는 새 터미널에서도 차단되면 같은 한 줄을 실행합니다. 조직의 그룹 정책으로 차단된 경우에는 `Get-ExecutionPolicy -List` 결과를 먼저 확인합니다.

## 0. MySQL 확인

```powershell
Get-Service MySQL80
```

`Running`이면 그대로 진행합니다. `Stopped`인 경우 관리자 PowerShell에서 `Start-Service MySQL80`을 실행합니다.

## 1. 터미널 1 — Kafka 노드 1

```powershell
cd C:\MLO01-01\Chapter3\Game-server
.\tools\kafka.ps1 -Action start -Node 1
```

로그가 나오는 상태로 둡니다. 아직 노드 2·3이 없어서 기다리는 로그가 잠깐 나올 수 있습니다.

## 2. 터미널 2 — Kafka 노드 2

```powershell
cd C:\MLO01-01\Chapter3\Game-server
.\tools\kafka.ps1 -Action start -Node 2
```

## 3. 터미널 3 — Kafka 노드 3

```powershell
cd C:\MLO01-01\Chapter3\Game-server
.\tools\kafka.ps1 -Action start -Node 3
```

## 4. 터미널 4 — 확인 후 Django 서버

```powershell
cd C:\MLO01-01\Chapter3\Game-server
.\tools\kafka.ps1 -Action status
.\tools\kafka.ps1 -Action describe-topic
.\server\.venv\Scripts\python.exe .\server\manage.py check
.\server\.venv\Scripts\python.exe .\server\manage.py runserver --noreload 127.0.0.1:8000
```

확인 기준: CurrentVoters 1·2·3, Topic 파티션 3개, 복제계수 3, ISR에 세 노드, Django 오류 없음.

## 5. 터미널 5 — 이벤트 publisher

```powershell
cd C:\MLO01-01\Chapter3\Game-server
.\server\.venv\Scripts\python.exe .\server\manage.py publish_game_events
```

publisher는 한 개만 실행합니다. 새 행동이 없으면 출력 없이 기다리는 것이 정상입니다.

## 6. 터미널 6 — 게임 접속기

```powershell
cd C:\MLO01-01\Chapter3\Game-client
.\.venv\Scripts\python.exe main.py
```

기존 학습 계정으로 로그인합니다. 비밀번호 확인이 필요하면 개인 로컬 파일 `Game-server/server/.env.classroom`을 직접 확인하고 공유하지 않습니다.

가상환경 Python 경로를 직접 지정했으므로 별도 activate는 필수가 아닙니다. 다른 계정을 동시에 확인하려면 **새 터미널**에서 같은 접속기 명령을 실행하고 다른 계정으로 로그인합니다.

## 7. 터미널 7 — 이벤트 관찰, 필요할 때 실행

```powershell
cd C:\MLO01-01\Chapter3\Game-server
.\server\.venv\Scripts\python.exe .\server\manage.py watch_game_events --limit 10
.\tools\kafka.ps1 -Action group
```

이 명령은 계속 켜 두는 서버가 아닙니다. 최대 10건을 관찰하거나 새 레코드가 없으면 종료합니다. 처음 그룹 할당 동안 observed=0이면 다시 실행합니다. 이미 읽은 사실만 있다면 observed=0도 정상입니다.

과거 사실을 별도 그룹으로 관찰하는 수업 명령:

```powershell
.\server\.venv\Scripts\python.exe .\server\manage.py watch_game_events --group village-inspect-v1 --limit 10
```

이 그룹도 이번 검증에서 기존 한 건을 읽었으므로 다시 실행하면 새 사실이 없을 수 있습니다. 운영 그룹의 offset을 reset하지 않습니다.

## 마칠 때

1. 게임 창에서 로그아웃한 후 창을 닫습니다.
2. publisher 터미널(5)에서 Ctrl+C.
3. Django 터미널(4)에서 Ctrl+C.
4. Kafka 터미널(1·2·3) 각각에서 Ctrl+C 후 종료 로그를 기다립니다.

Kafka 설치, cluster 초기화, Topic 생성, 패키지 설치, 기존 migration은 완료되어 있습니다. 다음 교시마다 다시 할 필요가 없습니다. **format·Topic 삭제·DB 초기화는 하지 않습니다.**

현재 완료 범위는 10일차 전체와 11일차 4교시입니다. 11일차 5교시 이후의 공동 마을 presence/snapshot 코드는 다음 작업 범위입니다.
