# Kafka와 Spark 한 번에 실행

PowerShell 또는 VS Code 터미널에서:

```powershell
cd C:\MLO01-01\Chapter3\Game-server
.\start-dev.cmd
```

탐색기에서 `start-dev.cmd`를 더블 클릭해도 된다. 서비스마다 별도 PowerShell 창이 열린다.

| 창 | 주소/포트 |
| --- | --- |
| Kafka 노드 1 | 9092 / controller 9093 |
| Kafka 노드 2 | 9094 / controller 9095 |
| Kafka 노드 3 | 9096 / controller 9097 |
| Spark Master | spark://127.0.0.1:7077 / 웹 화면 http://127.0.0.1:8080 |
| Spark Worker 1 | 7078 / 웹 화면 8081 / 1코어·2GB |
| Spark Worker 2 | 7079 / 웹 화면 8082 / 1코어·2GB |

Spark는 기존 `C:\MLO01-01\Chapter3\Oder-insight\Spark-exam\spark-4.1.3-bin-hadoop3-1` 설치본을 사용한다. Worker 작업 폴더는 이 설치본의 `work/worker-1`, `work/worker-2`로 분리한다. Worker는 Master 포트가 열리기를 최대 90초 기다린다.

각 창에 오류가 없는지 확인하고 Spark 웹 화면에서 Alive Workers가 2인지 확인한다. 창을 열었다는 안내는 서비스 시작 성공을 뜻하지 않는다. Kafka는 기존 `tools/kafka.ps1 -Action status` 및 `-Action describe-topic`으로 확인할 수 있다.

이미 사용 중인 포트는 건너뛴다. 다른 프로그램이 점유했을 수도 있으므로 기존 터미널을 확인한다. 실행 파일을 연속으로 눌러도 같은 체크아웃의 같은 서비스 실행 창은 중복으로 시작하지 않는다.

PowerShell 실행 정책은 이 실행 프로세스에서만 Bypass로 지정한다. PC나 사용자 계정의 영구 정책은 바꾸지 않는다. 조직 그룹 정책으로 금지된 경우 이를 해제하는 도구는 아니다.

**Django·publisher·게임 접속기는 이 파일에서 실행하지 않는다.** Kafka 데이터 초기화, 토픽 생성/삭제, migration, Spark 집계도 실행하지 않는다. 게임 이벤트 자동 발행이 필요하면 기존 publisher를 따로 실행한다.

## 종료

1. 진행 중인 Spark 집계가 끝날 때까지 기다린다.
2. Spark Worker 두 창에서 각각 Ctrl+C를 누른다.
3. Spark Master 창에서 Ctrl+C를 누른다.
4. 별도로 켜 둔 publisher가 있다면 먼저 종료하고, Kafka 세 창에서 각각 Ctrl+C를 누른 뒤 종료 로그를 기다린다.
5. 남은 PowerShell 창을 닫는다.

오류로 끝난 창은 내용을 읽을 수 있도록 남아 있다. 문제를 수정한 뒤 `start-dev.cmd`를 다시 실행하면 실행 중인 서비스는 유지하고 꺼진 서비스를 다시 연다.

## 서버를 켜지 않고 파일·포트만 검사

```powershell
.\start-dev.cmd -CheckOnly
```

Kafka/Spark 파일, Kafka 기존 저장소, Java 명령 존재 여부와 포트 점유를 확인한다. 실제 Kafka 클러스터나 Spark Worker 등록까지 확인하는 검사는 아니다.
